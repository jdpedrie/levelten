# Server configuration
PORT=3001

# Database configuration
# Path to the directory where the database file will be stored
# If left empty, defaults to './data' from the project root
# You can use absolute paths like '/var/lib/scorecard'
# or relative paths like './custom-data-dir'
DB_PATH=
# Name of the database file
DB_NAME=scorecard.db

# CORS configuration (for development)
# CLIENT_URL=http://localhost:3000